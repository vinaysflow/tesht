FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    fastapi \
    uvicorn[standard]

COPY gateway /app/gateway

ENV PYTHONPATH=/app

EXPOSE 9102

CMD ["python", "-m", "uvicorn", "gateway.sqlite_mcp_server:app", "--host", "0.0.0.0", "--port", "9102"]
