target-version = "py311"
line-length = 120

[lint]
# Explicit select so CI is not broken by Ruff default-rule expansions (0.16+).
select = ["E4", "E7", "E9", "F"]

[lint.per-file-ignores]
"backend/tools/verifier_cli.py" = ["E402"]
"backend/tests/conftest.py" = ["E402"]
